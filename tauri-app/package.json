{
  "name": "sap-odata-explorer-app",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "css": "node scripts/build-css.cjs",
    "dev": "npm run css && npx serve src -l 1420",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "@tauri-apps/cli": "^2",
    "tailwindcss": "^4.2.2"
  }
}
