{
  "name": "sap-odata-explorer-app",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "css": "npx tailwindcss -i src/input.css -o src/style.css --minify",
    "dev": "npm run css && npx serve src -l 1420",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "tailwindcss": "^3.4.19"
  }
}
